<h3>Results</h3>
<%= table :class => "results", :id => :results do %>
  <tr>
    <% race.result_columns_or_default_for_editing.each do |column| -%>
    <th><%= result_header column %></th>
    <% end -%>
    <th class="actions"></th>
  </tr>
  <%= render :partial => "result", :collection => race.results.sort %>
  <%= render :partial => "result", :collection => [race.results.build] %>
<% end unless race.new_record? -%>
