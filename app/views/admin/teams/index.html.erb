<% @page_title = 'Admin: Teams' %>

<div class="container-fluid">
  <h2>Teams</h2>
  <%= render :partial => "shared/search", :locals => { :name => @name, :placeholder => "Enter name to search" } %>

  <div class="row-fluid">
    <%= table :class => "teams", :id => :teams do %>
      <tr>
        <th class="icon"/>
        <th class="name">Name</th>
        <th>Aliases</th>
        <th>Member</th>
        <th class="last actions"></th>
      </tr>
      <%= render :partial => "team", :collection => @teams %>
      <% if @teams.empty? %>
      <tr>
        <%- if @name.blank? -%>
        <td colspan="5" class="not_found">Enter part of a team's name</td>
        <%- else -%>
        <td colspan="5" class="not_found">No teams found</td>
        <%- end -%>
      </tr>
      <% end %>
    <% end -%>
  </div>

  <div class="row-fluid">
    <%= link_to image_tag("icons/group_add.png"), new_admin_team_path, :class => "image new", :id => "new_team" %>
  </div>
</div>
