<% @page_title = "Teams" %>

<h2>OBRA-registered teams in Oregon and SW Washington.</h2>

<p>A list of <%= link_to "clubs rides", "http://#{RacingAssociation.current.static_host}/teams/rides.html", :class => "obvious" %> is available if you're looking for a group ride.</p>

<%= table :caption => "Teams", :id => "teams", :collection => @teams, :columns => 4 do %>
  <tr>
    <th>Name</th>
    <th>Sponsors</th>
    <th>Contact</th>
    <th>Results</th>
  </tr>
  <%= render :partial => "team", :collection => @teams.sort { |x, y| x.name.downcase <=> y.name.downcase } %>
<% end -%>

